# 오케스트레이션 5장: 스터디원 배포용 읽기 스크립트

이 문서는 Figma Slides 16장을 순서대로 보면서 읽는 자료입니다. 발표자가 없어도
각 장표의 제목, 그림, 코드, 실행 결과를 연결해 이해할 수 있도록 작성했습니다.

## 시작하기 전

이 장의 핵심은 간단합니다. AI 에이전트는 모델 하나가 아니라, 사용자의 요청을 받아
모델 호출, 도구 선택과 실행, 상태 관리, 컨텍스트 구성을 연결하는 **실행 구조**입니다.
이 실행 구조를 설계하는 일을 오케스트레이션이라고 합니다.

코드는 실제 LLM 서비스가 아니라 학습용 모형입니다. 실제 서비스의 LLM, CRM, DB,
외부 API 자리에 작은 로컬 함수를 두어 실행 순서와 판단을 눈에 보이게 만들었습니다.

```bash
python3 agent_orchestration_study_example.py
```

각 장표를 읽은 뒤, 굵게 표시한 확인 질문에 답해 보면 이해가 잘 되었는지 확인할 수
있습니다.

---

## 1. AI 에이전트 오케스트레이션

### 장표에서 볼 것

표지의 핵심 문장은 “모델, 도구, 컨텍스트, 실행 흐름을 연결하는 설계”입니다.

### 읽을 스크립트

이번 장은 어떤 모델이 가장 똑똑한지를 비교하는 내용이 아닙니다. 모델이 사용자
요청을 받았을 때, 어떤 도구를 골라야 하는지, 도구는 어떤 순서로 실행해야 하는지,
실행 결과는 다음 단계에 어떻게 전달해야 하는지를 다룹니다.

예를 들어 “기업 고객의 환불 요청을 처리해 줘”라는 문장은 한 번의 답변으로 끝나지
않을 수 있습니다. 고객 등급을 조회하고, 환불 정책을 찾고, 승인이 필요한지 확인한
뒤, 필요한 정보를 모아 답변해야 합니다. 이 전체 흐름을 조율하는 구조가
오케스트레이션입니다.

### 확인 질문

**에이전트와 모델을 같은 말로 부르면 왜 부족할까요?**

모델은 판단에 참여하는 한 구성 요소이고, 에이전트는 모델과 도구와 상태를 연결하는
전체 실행 구조이기 때문입니다.

### 다음 장으로

그렇다면 이 실행 구조는 실제로 어떤 일을 맡는지 보겠습니다.

---

## 2. 오케스트레이션의 역할

### 장표에서 볼 것

사용자 요청이 오케스트레이터를 거쳐 모델, 도구, 데이터로 이어지는 그림입니다.

### 읽을 스크립트

오케스트레이터는 단순히 “도구 A 다음에 도구 B를 호출하는 코드”가 아닙니다. 먼저
사용자 요청을 보고 지금 무엇이 필요한지 판단합니다. 그다음 관련 도구를 고르고,
도구의 실행 결과를 상태에 저장하며, 다음 모델 호출에 필요한 정보만 다시 조립합니다.

이 구조가 없으면 모델은 이전에 무엇을 했는지, 어떤 데이터가 최신인지, 어떤 도구가
실패했는지를 안정적으로 알기 어렵습니다. 따라서 오케스트레이터는 요청을 실제 작업
흐름으로 바꾸는 중앙 제어 로직이라고 볼 수 있습니다.

### 코드 연결

`graph_workflow`는 요청을 `billing` 또는 `technical`로 분류하고 상태 사전을
갱신합니다. `build_context`는 이 상태를 다음 모델 호출에 넣을 수 있는 형태로
만듭니다.

### 확인 질문

**오케스트레이터가 상태를 관리하지 않으면 다음 단계에서 어떤 문제가 생길까요?**

이전 도구 결과를 잃거나, 같은 조회를 반복하거나, 이미 실패한 작업을 성공한 것처럼
처리할 수 있습니다.

### 다음 장으로

여기서 반드시 구분해야 할 세 가지 책임이 있습니다.

---

## 3. 모델, 런타임, 도구의 책임 경계

### 장표에서 볼 것

모델, 런타임 또는 오케스트레이터, 도구 또는 외부 시스템의 세 칸을 비교합니다.

### 읽을 스크립트

가장 흔한 오해는 “모델이 API를 실행한다”는 말입니다. 정확히는 모델이 필요한 도구와
인자를 담은 **도구 호출 요청**을 만들 수 있습니다. 실제 API 호출은 애플리케이션의
런타임 또는 오케스트레이터가 수행합니다.

런타임은 요청한 인자가 형식에 맞는지, 사용자가 그 도구를 쓸 권한이 있는지, 외부 API가
얼마나 오래 응답을 기다리게 할지, 실패하면 재시도할지 판단합니다. 도구는 CRM, DB,
정책 문서 검색, 결제 시스템처럼 실제 정보를 가져오거나 변경하는 외부 작업을 합니다.

따라서 안전성과 감사 가능성은 프롬프트 문장만 잘 쓴다고 해결되지 않습니다. 권한
검증, 타임아웃, 재시도, 로그가 런타임에 구현되어야 합니다.

### 코드 연결

이 예제는 LLM 호출을 생략했습니다. 대신 `reflex_agent`와 `graph_workflow`가 모델의
간단한 판단 역할을 흉내 내고, `lookup_policy`, `get_customer_profile` 같은 함수가
도구 역할을 합니다.

### 확인 질문

**환불 요청에 사람 승인이 필요하다는 규칙은 모델 프롬프트에만 넣으면 충분할까요?**

충분하지 않습니다. 실제 변경을 막거나 승인 요청을 기록하는 동작은 런타임의 정책으로
강제해야 합니다.

### 다음 장으로

이제 모든 요청을 같은 방식으로 처리하지 않는 이유를 보겠습니다.

---

## 4. 에이전트 유형 스펙트럼

### 장표에서 볼 것

반사형, ReAct, 계획 후 실행, 쿼리 분해, 성찰형, 심층 리서치가 왼쪽에서 오른쪽으로
배치되어 있습니다.

### 읽을 스크립트

왼쪽의 반사형 에이전트는 조건이 맞으면 바로 행동합니다. 빠르고 예측 가능하지만,
복잡한 문제를 여러 단계로 나누거나 스스로 오류를 점검하지는 못합니다. 오른쪽으로
갈수록 계획, 검색, 검증, 재계획 같은 능력이 추가됩니다. 대신 모델 호출 수와 지연시간,
운영 복잡도도 커집니다.

여기서 중요한 것은 오른쪽이 항상 더 좋다는 뜻이 아니라는 점입니다. 비밀번호 재설정
상태 조회처럼 단순하고 위험이 낮은 일은 반사형 또는 정해진 워크플로가 더 적합할 수
있습니다. 반대로 의료, 법률, 고액 결제처럼 틀렸을 때 비용이 큰 일은 검증과 사람 승인이
필요할 수 있습니다.

### 코드 연결

`reflex_agent`는 5.1.1 반사형 에이전트의 축소판입니다. `calculate:` 또는 `refund`라는
조건이 보이면 계획 루프 없이 즉시 함수를 실행합니다.

```bash
python3 agent_orchestration_study_example.py --section reflex
```

### 확인 질문

**빠른 반사형이 적합한 요청과 부적합한 요청을 하나씩 떠올릴 수 있나요?**

### 다음 장으로

에이전트 유형은 취향이 아니라 요구사항으로 골라야 합니다.

---

## 5. 에이전트 유형 선택 기준

### 장표에서 볼 것

지연시간, 정확도, 비용, 감사 가능성 네 기준입니다.

### 읽을 스크립트

설계를 시작할 때 “어떤 프레임워크를 쓸까?”보다 먼저 물어야 할 질문이 있습니다.
사용자가 얼마나 기다릴 수 있는가, 틀렸을 때 피해가 얼마나 큰가, 호출 비용을 얼마나
허용하는가, 나중에 왜 이런 답을 했는지 설명해야 하는가입니다.

예를 들어 고객에게 단순 배송 현황을 알려 주는 기능은 빠른 응답이 더 중요할 수
있습니다. 반면 환불이나 계약 변경은 한 번의 잘못된 실행이 금전적 문제를 만들 수
있으므로, 추가 검증과 사람 승인을 넣는 편이 좋습니다. 즉 패턴 선택은 기술의 화려함이
아니라 제품의 실패 비용과 운영 요구사항의 결과입니다.

### 확인 질문

**우리 서비스에서 ‘틀렸을 때 비용이 큰’ 작업은 무엇이고, 거기에는 어떤 검증이
필요할까요?**

### 다음 장으로

다음으로는 도구 수가 늘어날 때 선택 구조가 어떻게 달라지는지 보겠습니다.

---

## 6. 도구 선택 전략

### 장표에서 볼 것

표준 선택, 시맨틱 선택, 계층적 선택의 세 가지 전략입니다.

### 읽을 스크립트

도구가 아주 적을 때는 모델에게 전체 도구 목록을 주고 고르게 해도 됩니다. 이것이
표준 선택입니다. 구현이 단순하고 디버깅하기 쉽습니다.

도구가 수십 개에서 수백 개로 늘어나면 매번 모든 설명을 모델에 넣기 어렵습니다.
이때 질문과 도구 설명의 의미를 비교해 관련 후보만 먼저 가져오는 방식이 시맨틱
선택입니다. 도구가 도메인별로 잘 나뉜 경우에는 먼저 고객, 지원, 결제 같은 큰 그룹을
고르고 그 안에서 세부 도구를 고르는 계층적 선택도 유용합니다.

### 코드 연결

`semantic_tool_selection`은 관련 후보를 먼저 줄이는 구조를, `hierarchical_tool_selection`
은 그룹을 먼저 고른 뒤 그 안의 도구만 살피는 구조를 보여 줍니다.

### 확인 질문

**도구가 5개일 때와 100개일 때 같은 선택 전략을 사용하면 어떤 비용이 생길까요?**

### 다음 장으로

후보를 먼저 줄인다는 말이 실제로 어떤 뜻인지 다음 장에서 보겠습니다.

---

## 7. 도구 선택은 후보 공간을 줄이는 일

### 장표에서 볼 것

전체 도구 카탈로그에서 검색과 필터를 거쳐 상위 후보, 최종 선택으로 가는 흐름입니다.

### 읽을 스크립트

도구 선택은 한 번의 마법 같은 결정이 아닙니다. 보통은 전체 도구 목록에서 사용자
질문과 관련 있는 후보를 먼저 찾습니다. 그다음 권한, 현재 사용 사례, 입력 가능한
인자 같은 조건으로 다시 거르고, 마지막에 모델이 어떤 도구를 어떤 인자로 호출할지
결정합니다.

실습 코드에서는 이 과정을 투명하게 보여 주기 위해 `score_tool`이 질문의 단어와 도구
설명의 단어가 얼마나 겹치는지 점수로 출력합니다. 이것은 실제 시맨틱 검색이 아닙니다.
실제 서비스에서는 임베딩, 벡터 검색, 메타데이터 필터를 사용해 단어가 달라도 의미상
가까운 도구를 찾습니다.

### 코드 실행

```bash
python3 agent_orchestration_study_example.py --section semantic
```

출력에서 `customer_profile`과 `order_history`의 점수가 높은 이유를 읽어 보세요. 이어서
`human_review`가 낮은 점수로 후보에 들어오는 것도 확인할 수 있습니다. 이 사례는 단어
점수 기반 모형의 한계를 보여 줍니다.

### 확인 질문

**이 코드의 점수 방식이 실제 임베딩 검색과 다른 이유는 무엇인가요?**

### 다음 장으로

후보를 잘 줄이려면 도구 설명 자체도 정확해야 합니다.

---

## 8. 좋은 도구 설명

### 장표에서 볼 것

모호한 `process_numbers`와 구체적인 `calculate_sum` 설명의 대비입니다.

### 읽을 스크립트

도구 설명은 개발자만 보는 주석이 아닙니다. 모델이 어떤 도구를 선택할지 판단하는
입력입니다. 따라서 함수 이름만 보고는 알 수 없는 목적, 입력 형식과 범위, 반환 결과,
사용 예시를 써야 합니다.

`숫자를 처리합니다`라는 설명은 너무 넓습니다. 합계를 구하는지, 평균을 구하는지,
문자열도 받을 수 있는지 알 수 없습니다. 반면 “두 정수 x와 y의 합을 반환한다”처럼
구체적으로 쓰면 모델과 사람이 같은 계약을 이해할 수 있습니다. 비슷한 도구가 많을수록
이 설명의 품질이 도구 오선택을 크게 좌우합니다.

### 코드 연결

`Tool` 데이터 클래스의 `name`, `description`, `group`이 바로 이 계약의 단순화된
형태입니다. `score_tool`도 이 설명을 읽어 후보 점수를 만듭니다.

### 확인 질문

**도구 설명에 입력 예시를 넣으면 선택 품질이 왜 좋아질까요?**

### 다음 장으로

도구가 선택된 뒤에는 자연어를 실제 실행 가능한 입력으로 바꿔야 합니다.

---

## 9. 도구 실행에서 필요한 것

### 장표에서 볼 것

“뉴욕 날씨 알려 줘”가 `{ city: "New York City" }`라는 인자로 바뀌고, 타입 검증,
타임아웃, 재시도가 뒤따르는 흐름입니다.

### 읽을 스크립트

사용자 언어는 모호하고 도구 입력은 엄격합니다. “뉴욕 날씨 알려 줘”라는 문장을 API가
바로 이해하지 못할 수 있으므로, 도시 이름을 표준화하고 필요한 형식의 인자로 만들어야
합니다. 이 과정을 파라미터화 또는 인자 추출이라고 합니다.

실무에서 중요한 점은 외부 도구가 항상 성공하지 않는다는 것입니다. API는 느려지거나,
잘못된 형식의 데이터를 받거나, 권한 오류를 낼 수 있습니다. 따라서 런타임은 입력을
검증하고, 제한 시간을 두며, 재시도 여부를 정하고, 최종 실패를 사용자에게 설명할 수
있어야 합니다.

### 코드 연결

예제는 외부 API 대신 문자열과 로컬 함수를 사용하므로 오류 상황을 실제로 발생시키지는
않습니다. 대신 `safe_calculate`는 허용한 산술 연산만 처리해, 입력을 무제한 실행하지
않는 검증의 작은 예를 보여 줍니다.

### 확인 질문

**모델이 만든 인자를 그대로 외부 결제 API에 넘기면 어떤 위험이 있을까요?**

### 다음 장으로

도구를 실행할 때는 도구 사이의 의존성에 맞는 구조를 골라야 합니다.

---

## 10. 도구 토폴로지

### 장표에서 볼 것

단일 실행, 병렬 실행, 체인, 그래프의 네 가지 연결 모양입니다.

### 읽을 스크립트

도구 토폴로지는 도구들이 어떤 순서와 의존성으로 연결되는지 뜻합니다. 한 번의 조회면
단일 실행이면 충분합니다. 고객 정보, 주문 내역, 티켓 로그처럼 서로 결과를 기다리지
않는 조회는 병렬로 시작할 수 있습니다.

앞 단계 결과가 다음 단계의 입력이 되면 체인입니다. 예를 들어 요청을 분류한 뒤, 분류
결과에 맞는 정책을 조회하는 흐름입니다. 조건에 따라 서로 다른 경로로 가고 나중에
합쳐야 하면 그래프가 더 잘 맞습니다. 그래프는 표현력이 좋지만, 가능한 경로가 많아져
테스트와 관찰이 더 중요해집니다.

### 코드 연결

`parallel_tool_execution`은 병렬, `chain_workflow`는 체인, `graph_workflow`는 그래프를
각각 보여 줍니다.

```bash
python3 agent_orchestration_study_example.py --section parallel
```

출력의 `profile`, `orders`, `ticket`, `policy` 네 항목은 서로를 기다리지 않는 독립 조회
결과입니다.

### 확인 질문

**주문 내역을 받아야만 할인율을 계산할 수 있다면 병렬과 체인 중 무엇이 맞을까요?**

### 다음 장으로

체인과 그래프의 차이를 같은 환불 요청으로 더 구체적으로 비교해 보겠습니다.

---

## 11. 같은 요청도 실행 구조는 달라질 수 있습니다

### 장표에서 볼 것

왼쪽은 고정 순서의 체인이고, 오른쪽은 `Enterprise?` 조건에 따라 human review로
분기하는 그래프입니다.

### 읽을 스크립트

체인은 “항상 A 다음 B, B 다음 C”처럼 경로가 고정될 때 읽기 쉽고 구현도 단순합니다.
하지만 기업 고객인지 여부에 따라 사람이 승인해야 하는 경우처럼 다음 단계가 달라지면,
체인만으로 표현하면 예외 처리 코드가 여기저기 섞일 수 있습니다.

그래프는 상태를 들고 가며 조건에 따라 다른 경로로 보낼 수 있습니다. 이 예제에서는
요청에 `refund` 또는 `invoice`가 있으면 `billing` 경로로 보냅니다. 그 뒤 `enterprise`
가 있으면 `human_review` 결과를 추가하고, 마지막에는 어느 경로였든 하나의 `response`
필드로 결과를 합칩니다.

### 코드 실행

```bash
python3 agent_orchestration_study_example.py --section graph
```

출력의 `issue_type`, `step_result`, `response`를 순서대로 보세요. 특히
`Human review requested`가 왜 추가됐는지 장표 오른쪽의 분기와 연결해 설명할 수 있어야
합니다.

### 확인 질문

**그래프가 체인보다 항상 좋은 선택이 아닌 이유는 무엇일까요?**

경로가 늘어나면 테스트 케이스, 오류 처리, 로그 분석의 범위도 함께 늘어나기 때문입니다.

### 다음 장으로

도구 실행이 끝난 뒤에는 그 결과를 다음 모델 호출에 넣을 수 있게 정리해야 합니다.

---

## 12. 컨텍스트 엔지니어링

### 장표에서 볼 것

컨텍스트 윈도우 안에 시스템 지침, 사용자 요청, 검색 지식, 도구 결과, 워크플로 상태가
들어가는 그림입니다.

### 읽을 스크립트

프롬프트 엔지니어링이 “모델에게 어떤 지시문을 쓸까”에 가깝다면, 컨텍스트 엔지니어링은
“이번 호출에서 모델이 알아야 할 정보를 무엇으로 구성할까”에 가깝습니다.

모델은 이전 단계의 결과를 자동으로 모두 기억하는 존재가 아닙니다. 따라서 시스템 규칙,
현재 사용자 요청, 이미 실행한 도구의 결과, 현재 워크플로 상태, 검색으로 찾은 근거를
명시적으로 넣어야 합니다. 단, 무조건 많이 넣는다고 좋아지지는 않습니다. 관련 없는
정보는 모델의 주의를 분산하고 비용과 지연시간을 키울 수 있습니다.

### 코드 연결

`build_context`는 `SYSTEM`, `USER`, `WORKFLOW_STATE`, `RETRIEVED_KNOWLEDGE` 순서로
문자열을 조립합니다. 실제 서비스에서는 이 문자열이 다음 모델 호출의 입력 일부가 됩니다.

### 확인 질문

**프롬프트를 길게 쓰는 것과 컨텍스트를 잘 설계하는 것은 어떻게 다를까요?**

### 다음 장으로

컨텍스트에는 길이 제한이 있으므로 우선순위가 필요합니다.

---

## 13. 컨텍스트는 예산 안에서 우선순위를 정한다

### 장표에서 볼 것

SYSTEM, USER, STATE, KNOWLEDGE 네 블록과 “현재 목표, 최신 상태, 근거, 압축”이라는
우선순위입니다.

### 읽을 스크립트

컨텍스트 윈도우는 무한한 메모장이 아니라 제한된 예산입니다. 그래서 중요한 정보를 먼저
넣고, 덜 중요한 정보는 압축하거나 생략해야 합니다. 먼저 역할과 안전 규칙을 넣고,
현재 사용자가 무엇을 원하는지 넣습니다. 그다음 지금까지의 실행 상태와 답변의 근거가
되는 검색 지식을 넣습니다.

실습 코드의 `max_chars=900`은 토큰 예산을 아주 단순하게 문자 수로 바꾼 것입니다.
`build_context_with_trace`는 지식 조각을 하나씩 넣어 보고 제한을 넘으면 제외합니다.
실무에서는 문자 수가 아니라 토큰 수, 정보의 최신성, 출처의 신뢰도, 현재 목표와의
관련성을 함께 고려합니다.

### 코드 실행

```bash
python3 agent_orchestration_study_example.py --section context
```

출력에서 네 블록의 순서와, 포함된 검색 지식 개수를 확인하세요. 이 문자열을 보고
“모델이 지금 무엇을 알고 답변하게 되는가”를 설명할 수 있어야 합니다.

### 확인 질문

**긴 대화 전체를 항상 컨텍스트에 넣지 않는 이유는 무엇일까요?**

### 다음 장으로

이제 지금까지의 개념이 코드에 어떻게 대응되는지 한 번에 보겠습니다.

---

## 14. 예제 코드 시연

### 장표에서 볼 것

일곱 함수와 반사형, 선택, 병렬, 체인, 그래프, 컨텍스트의 대응표입니다.

### 읽을 스크립트

이 코드는 완성된 AI 고객지원 서비스가 아닙니다. 오케스트레이션의 각 설계 결정을
분리해서 관찰하는 학습용 모형입니다. 실제 서비스에서는 단순 규칙 자리에 모델 호출이
들어가고, 로컬 함수 자리에 DB와 외부 API가 들어갑니다.

코드는 위에서 아래로 보면 좋습니다. `Tool`은 도구의 이름, 설명, 실행 함수, 그룹을
정의합니다. `reflex_agent`는 조건 기반 즉시 실행을, 도구 선택 함수들은 후보 축소를,
`parallel_tool_execution`은 독립 작업의 동시 실행을 보여 줍니다. `chain_workflow`와
`graph_workflow`는 흐름 구조의 차이를 보여 주고, 마지막 `build_context`는 다음 모델
호출 직전 정보를 조립합니다.

### 권장 실습 순서

```bash
python3 agent_orchestration_study_example.py --section reflex
python3 agent_orchestration_study_example.py --section semantic
python3 agent_orchestration_study_example.py --section parallel
python3 agent_orchestration_study_example.py --section graph
python3 agent_orchestration_study_example.py --section context
python3 -m unittest -v
```

전체 실행은 가능하지만, 한 번에 모든 출력을 보면 개념이 섞일 수 있습니다. 위 순서대로
한 개념씩 실행하고, 장표의 그림과 출력의 필드를 연결하는 편이 좋습니다.

### 확인 질문

**이 예제에서 실제 서비스와 다른 부분을 세 가지 이상 찾을 수 있나요?**

정답 예시는 규칙 기반 판단, 단어 겹침 점수, 로컬 데이터 함수, 문자 수 제한입니다.

### 다음 장으로

마지막으로 실제 제품에서 구조를 선택할 때의 체크리스트를 정리하겠습니다.

---

## 15. 실무 설계 체크리스트

### 장표에서 볼 것

지연시간과 정확도, 액션 수, 계획 변화, 테스트, 가장 단순한 구조라는 다섯 항목입니다.

### 읽을 스크립트

실무에서 좋은 설계는 가장 많은 기능을 넣은 설계가 아닙니다. 요구사항을 만족하면서도
이해하고 테스트하고 운영하기 쉬운 가장 단순한 구조입니다.

먼저 응답 시간과 정확도 요구사항을 정합니다. 다음으로 한 요청에 보통 몇 번의 행동이
필요한지 봅니다. 계획이 자주 바뀌거나 조건 분기가 많은지 평가합니다. 그 뒤 대표적인
정상 경로와 실패 경로를 테스트해 봅니다. 이 과정을 거치면 굳이 그래프가 필요 없는
문제를 그래프로 과하게 만들지 않을 수 있습니다.

### 확인 질문

**지금 만들고 있는 기능은 반사형, 체인, 그래프 중 어디에서 시작하는 것이 가장
단순할까요? 그 이유는 무엇인가요?**

### 다음 장으로

마지막 질문을 각자의 프로젝트에 대입하며 장을 마무리하겠습니다.

---

## 16. 토론 질문

### 장표에서 볼 것

반사형으로 충분한 기능, 도구 5개와 100개의 차이, 그래프 테스트, 컨텍스트 선별에 관한
네 가지 질문입니다.

### 읽을 스크립트

이 장의 목표는 특정 프레임워크나 패턴을 외우는 것이 아닙니다. 각자의 서비스에서
요청의 위험도와 복잡도를 보고 적절한 실행 구조를 선택할 수 있게 되는 것입니다.

첫 번째 질문에서는 복잡한 추론 없이도 처리 가능한 기능을 찾습니다. 두 번째 질문에서는
도구 수가 늘 때 검색과 계층이 왜 필요한지 생각합니다. 세 번째 질문에서는 그래프의
분기마다 무엇을 테스트하고 로그로 남겨야 하는지 확인합니다. 마지막 질문에서는 현재
모델 호출에 정말 필요한 정보와 제외해도 되는 정보를 구분합니다.

이 네 질문에 답할 수 있다면, 이번 장의 핵심인 “에이전트는 모델이 아니라 실행 구조”를
자신의 프로젝트에 적용할 준비가 된 것입니다.

### 마무리 문장

에이전트의 품질은 모델의 능력만으로 결정되지 않습니다. 어떤 도구를 안전하게 고르고,
어떤 순서로 실행하고, 어떤 상태와 근거를 다음 호출에 전달하는지가 제품의 신뢰성을
결정합니다.
