# 발표자용 가이드: 오케스트레이션 5장

이 문서는 Figma Slides의 발표 흐름과 `agent_orchestration_study_example.py`를
연결합니다. 슬라이드는 한 메시지에 집중하고, 세부 설명은 발표자가 이 문서와
발표자 노트로 전달하는 구성을 권장합니다.

## 1. 발표 전체 메시지

에이전트는 "모델 하나"가 아닙니다. 사용자 요청을 이해한 뒤, 필요한 도구를
고르고, 실행 순서와 상태를 관리하며, 다음 모델 호출에 필요한 정보를 조립하는
**실행 구조**입니다. 이 구조를 설계하는 일이 오케스트레이션입니다.

## 2. 장표별 핵심 메시지와 말할 내용

| 장표 | 청중이 기억할 한 문장 | 발표자가 풀어 줄 내용 |
| --- | --- | --- |
| 1 | 오케스트레이션은 모델과 도구를 연결하는 실행 설계다. | 오늘은 모델 성능이 아니라, 요청이 실제 작업으로 바뀌는 경로를 본다고 선언한다. |
| 2 | 오케스트레이터는 계획, 도구 선택, 실행, 상태 전달을 맡는다. | 사용자 요청이 모델, 도구, 데이터로 흘러가는 큰 그림을 먼저 잡아 준다. |
| 3 | 모델은 요청하고, 런타임은 검증·실행하며, 도구는 결과를 돌려준다. | 모델이 API를 직접 실행한다는 흔한 오해를 바로잡고, 권한·재시도·로그가 런타임 책임임을 말한다. |
| 4 | 에이전트 유형은 좋고 나쁨이 아니라 요구사항 선택이다. | 반사형은 빠르고 예측 가능하지만 복잡한 재계획에는 약하다고 설명한다. |
| 5 | 지연시간, 실패 비용, 정확도, 감사 가능성이 패턴을 정한다. | "틀렸을 때 누가 얼마나 손해 보는가"를 먼저 묻는 선택 기준을 제시한다. |
| 6 | 도구가 많아지면 도구 선택도 검색과 라우팅 문제가 된다. | 전체 도구를 매번 넣는 방식에서 검색, 계층 구조로 확장되는 이유를 설명한다. |
| 7 | 도구 선택은 후보 공간을 줄인 뒤 최종 선택을 하는 과정이다. | 100개 도구에서 검색·필터를 거쳐 상위 후보만 남기는 이유와 실습의 단어 점수 한계를 설명한다. |
| 8 | 도구 설명은 모델이 읽는 API 계약서다. | 이름, 고유 목적, 입력 제약, 예시가 겹치는 도구의 오선택을 줄인다고 말한다. |
| 9 | 자연어를 실행 가능한 인자로 바꾼 뒤, 실패도 정상 흐름으로 다룬다. | 검증, 권한, 타임아웃, 재시도는 모델이 아니라 런타임 책임임을 강조한다. |
| 10 | 병렬, 체인, 그래프는 실행 의존성에 따라 고른다. | 독립이면 병렬, 순서 의존이면 체인, 분기와 재합류가 있으면 그래프라고 비교한다. |
| 11 | 같은 환불 요청도 분기 조건이 생기면 체인보다 그래프가 명확하다. | enterprise 여부에 따라 human review가 추가되는 코드를 예로 들고, 그래프의 테스트 비용도 함께 말한다. |
| 12 | 컨텍스트 엔지니어링은 모델에 줄 정보를 고르고 압축하는 일이다. | 프롬프트 문장만 다듬는 일과 달리, 상태와 검색 지식을 선별하고 순서를 정하는 일이라고 설명한다. |
| 13 | 컨텍스트는 예산 안에서 목표·상태·근거 순으로 채운다. | SYSTEM, USER, STATE, KNOWLEDGE의 역할과 긴 원문을 그대로 넣지 않는 이유를 설명한다. |
| 14 | 예제 코드는 각 설계 결정을 눈으로 확인하는 개념 모형이다. | 실제 LLM 대신 규칙과 로컬 함수로 구조를 드러냈음을 밝히고, 아래 시연 순서로 넘어간다. |
| 15 | 요구사항을 만족하는 가장 단순한 구조에서 시작한다. | 복잡한 그래프는 멋있어 보여도 테스트 경로와 운영 비용을 늘린다고 정리한다. |
| 16 | 우리 제품에 대입해 어떤 구조가 필요한지 토론한다. | "우리 서비스의 고위험 작업은 무엇인가"로 질문을 이어 발표를 닫는다. |

## 3. 코드 흐름: 입력부터 모델 호출 직전까지

```text
사용자 요청
  -> reflex_agent 또는 workflow가 요청의 성격을 판단
  -> semantic_tool_selection / hierarchical_tool_selection이 후보를 좁힘
  -> 병렬, 체인, 그래프 구조로 도구를 실행
  -> graph_workflow가 상태를 갱신하고 분기 결과를 합침
  -> build_context가 지침 + 요청 + 상태 + 검색 지식을 조립
  -> 실제 서비스라면 이 다음 모델 호출과 최종 응답 생성
```

## 4. 함수별 해설과 장표 연결

| 함수 | 입력 | 내부 동작 | 출력 | 연결 장표 |
| --- | --- | --- | --- | --- |
| `reflex_agent` | 한 줄 요청 | 규칙을 순서대로 검사한다. | 계산 결과 또는 정책 문장 | 4, 14 |
| `score_tool` | 요청, 도구 설명 | 요청 단어가 도구 이름/설명/그룹에 몇 번 등장하는지 센다. | 정수 점수 | 5, 10 |
| `semantic_tool_selection` | 요청, 상위 후보 수 | 점수를 기준으로 모든 도구를 정렬한 뒤 상위 후보만 남긴다. | `Tool` 목록 | 6, 7, 14 |
| `select_tool_group` | 요청 | 환불·고객·지원 키워드로 큰 도메인을 먼저 라우팅한다. | 그룹 이름 | 6, 7, 14 |
| `hierarchical_tool_selection` | 요청 | 선택된 그룹의 도구만 다시 정렬한다. | 더 작은 `Tool` 목록 | 6, 7, 14 |
| `parallel_tool_execution` | 고객 ID, 티켓 ID | 독립적인 네 조회를 `asyncio.gather`로 동시에 시작한다. | 고객·주문·티켓·정책 묶음 | 10, 14 |
| `chain_workflow` | 요청 | 정규화 -> 분류 -> 다음 액션을 순서대로 수행한다. | 단계별 결과 사전 | 10, 11, 14 |
| `graph_workflow` | 요청 | 상태를 만들고, billing/technical로 분기한 뒤 공통 응답을 만든다. | 갱신된 상태 사전 | 10, 11, 14 |
| `build_context_with_trace` | 요청, 상태, 검색 지식 | 중요 블록을 먼저 놓고, 길이 제한 안에 드는 지식만 추가한다. | 컨텍스트 문자열, 포함된 지식 | 12, 13, 14 |

## 5. 라이브 시연 절차

1. 전체 실행으로 제목과 7개 섹션을 빠르게 보여 준다.

   ```bash
   python3 agent_orchestration_study_example.py
   ```

2. `--section reflex`를 실행하고, "조건이 맞으면 모델의 반복 추론 없이 바로 실행한다"고 말한다.

3. `--section semantic`을 실행해 점수표를 보여 준다. 이 점수표는 진짜 임베딩이 아니라 구조를 눈에 보이게 만든 대체물이라고 밝힌다.

4. `--section parallel`을 실행하고, 출력 항목 네 개가 서로 입력을 기다리지 않는 독립 조회임을 짚는다.

5. `--section graph`를 실행하고, enterprise + refund 때문에 human review가 추가되는 분기를 읽어 준다.

6. `--section context`를 실행해 `SYSTEM`, `USER`, `WORKFLOW_STATE`, `RETRIEVED_KNOWLEDGE` 순서를 보여 준다. 이 문자열이 실제라면 다음 모델 호출의 입력이 된다.

## 6. 자주 나오는 질문에 대한 짧은 답

**이 코드가 LLM 에이전트인가요?**

아니요. LLM 호출을 생략한 구조 학습용 모형입니다. 실제 서비스에서는 규칙 부분에
모델의 도구 호출 결정이 들어가고, 로컬 데이터 함수가 API와 DB 호출로 바뀝니다.

**시맨틱 선택이 단어가 겹치는 것뿐인데 의미 검색인가요?**

이 예제에서는 아닙니다. 실제 시맨틱 선택은 보통 질의와 도구 설명을 임베딩해 의미상
가까운 후보를 찾습니다. 여기서는 선택 과정이 가려지지 않게 단어 점수로 바꿨습니다.

**병렬 실행은 항상 빠른가요?**

독립 작업일 때만 유리합니다. 서로 결과를 기다리는 작업을 억지로 병렬화하면 오류와
복잡도만 늘어납니다. 또한 외부 API의 동시성 제한도 고려해야 합니다.

**컨텍스트에 정보를 많이 넣으면 더 정확하지 않나요?**

아닙니다. 관련 없는 정보는 주의를 분산하고 비용과 지연시간을 늘립니다. 현재 목표와
직접 연결되는 정보, 최신 상태, 근거가 되는 지식부터 우선순위를 정해야 합니다.
